[project]
name = "avgaussianfusion"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "pyyaml",
  "soundfile",
  "torch",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
